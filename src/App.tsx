import styled, { keyframes } from "styled-components";
import Circle from "./circle.tsx";
function App() {
	return (
		<div>
			<Circle bgColor="teal" />
			<Circle bgColor="tomato" />
		</div>
	);
}

export default App;
